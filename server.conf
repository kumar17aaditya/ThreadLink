# ThreadLink server configuration.
# Lines starting with '#' and blank lines are ignored.
# Any missing or invalid value falls back to a safe default (see docs/PROTOCOL.md).

PORT=8080
MAX_CLIENTS=100
MAX_MESSAGE_SIZE=8192

# DEBUG | INFO | WARN | ERROR
LOG_LEVEL=INFO
